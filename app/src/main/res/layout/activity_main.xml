<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:background="@android:color/white"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.MainActivity">

    <!-- Израз -->
    <TextView
        android:id="@+id/tvExpression"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="32sp"
        android:textColor="#8A8A8A"
        android:padding="20dp"
        android:gravity="end" />

    <!-- Резултат -->
    <TextView
        android:id="@+id/tvResult"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="48sp"
        android:textColor="#000000"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingBottom="10dp"
        android:gravity="end" />

    <!-- Ред 1: C () % ÷ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="4dp">

        <Button android:id="@+id/btnClear" style="@style/CalcButtonGrey" android:text="C" />
        <Button android:id="@+id/btnBrackets" style="@style/CalcButtonGrey" android:text="()" />
        <Button android:id="@+id/btnPercent" style="@style/CalcButtonGrey" android:text="%" />
        <Button android:id="@+id/btnDivide" style="@style/CalcButtonGreen" android:text="÷" />
    </LinearLayout>

    <!-- Ред 2: 7 8 9 × -->
    <LinearLayout style="@style/CalcRow">
        <Button android:id="@+id/btn7" style="@style/CalcButtonWhite" android:text="7" />
        <Button android:id="@+id/btn8" style="@style/CalcButtonWhite" android:text="8" />
        <Button android:id="@+id/btn9" style="@style/CalcButtonWhite" android:text="9" />
        <Button android:id="@+id/btnMultiply" style="@style/CalcButtonGreen" android:text="×" />
    </LinearLayout>

    <!-- Ред 3: 4 5 6 - -->
    <LinearLayout style="@style/CalcRow">
        <Button android:id="@+id/btn4" style="@style/CalcButtonWhite" android:text="4" />
        <Button android:id="@+id/btn5" style="@style/CalcButtonWhite" android:text="5" />
        <Button android:id="@+id/btn6" style="@style/CalcButtonWhite" android:text="6" />
        <Button android:id="@+id/btnSubtract" style="@style/CalcButtonGreen" android:text="-" />
    </LinearLayout>

    <!-- Ред 4: 1 2 3 + -->
    <LinearLayout style="@style/CalcRow">
        <Button android:id="@+id/btn1" style="@style/CalcButtonWhite" android:text="1" />
        <Button android:id="@+id/btn2" style="@style/CalcButtonWhite" android:text="2" />
        <Button android:id="@+id/btn3" style="@style/CalcButtonWhite" android:text="3" />
        <Button android:id="@+id/btnAdd" style="@style/CalcButtonGreen" android:text="+" />
    </LinearLayout>

    <!-- Ред 5: +/- 0 . = -->
    <LinearLayout style="@style/CalcRow">
        <Button android:id="@+id/btnPlusMinus" style="@style/CalcButtonWhite" android:text="+/-" />
        <Button android:id="@+id/btn0" style="@style/CalcButtonWhite" android:text="0" />
        <Button android:id="@+id/btnDot" style="@style/CalcButtonWhite" android:text="." />
        <Button android:id="@+id/btnEquals" style="@style/CalcButtonGreen" android:text="=" />
    </LinearLayout>

</LinearLayout>
